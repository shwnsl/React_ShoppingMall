import { useQuery } from '@tanstack/react-query';
import React from 'react';

const Home = () => {

    return (
        <div className='home'>
            Home<br/>
            화면<br/>
            이다
        </div>
    );
};

export default Home;